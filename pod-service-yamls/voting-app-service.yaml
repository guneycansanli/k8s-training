apiVersion: v1
kind: Service
metadata:
  name: voting-service
  labels:
    name: voting-service
    app: demo-voting-app
spec:
  type: NodePort
  ports:
    - port: 80
      targerPort: 80
      nodePort: 3000
  selector:
    name: result-app-pod
    app: demo-voting-app
